#include <stdio.h>

int main() {
    int B = 98;
    int maxNumber = 15;

    // Выводим запрос с значением B
    printf("Введите B: %d\n", B);

    // Выводим строку с наибольшим числом
    printf("Наибольшее число: %d\n", maxNumber);

    return 0;
}
