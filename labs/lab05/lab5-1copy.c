
#include <stdio.h>

int main() {
    printf("Введите строку:\n");           // Выводим приглашение и переводим строку (\n)
    printf("Алексеев Тимофей\n");          // Выводим первую строку с переводом строки
    printf("Алексеев Тимофей\n");          // Выводим вторую строку с переводом строки
    return 0;
}
